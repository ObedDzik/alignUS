__pycache__/
*.pyc
*.pyo
wandb/
.DS_Store
*.egg-info/
venv/

# internal notes / process history — not for public consumption
CLAUDE.md

# internal experiment records from an unrelated research thread that got
# pulled in during vendoring — job IDs, scratch paths, not paper-relevant
# (see CLAUDE.md's "baseline/guideus/ — unresolved concern")
baseline/guideus/RESULTS.md
baseline/guideus/results_paper_metrics.json

# never commit real credentials, however they get in
.env
*.key
*secret*

# not for the public repo — obed's explicit ask (2026-09-23). resources/
# otherwise holds patient/cohort-identifying split files (bk_core_splits.json,
# bk_patient_splits.json, two tarek-SSL-project patient-list JSONs) that must
# stay private, but G7.npy is a real runtime dependency (a plain grayscale
# lookup table, no patient data -- medAI.datasets.nct2013.utils.apply_colormap
# reads it on every B-mode image load) and is explicitly un-ignored below.
resources/*
!resources/G7.npy
figures/
debug.sh
align_us.pdf
