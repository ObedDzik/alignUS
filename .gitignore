__pycache__/
*.pyc
*.pyo
wandb/
.DS_Store
*.egg-info/

# internal notes / process history — not for public consumption
CLAUDE.md

# internal experiment records from an unrelated research thread that got
# pulled in during vendoring — job IDs, scratch paths, not paper-relevant
# (see CLAUDE.md's "baseline/guideus/ — unresolved concern")
baseline/guideus/RESULTS.md
baseline/guideus/results_paper_metrics.json

# never commit real credentials, however they get in
.env
*.key
*secret*

# not for the public repo — obed's explicit ask (2026-09-23)
resources/
figures/
debug.sh
align_us.pdf
